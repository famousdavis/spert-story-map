{
  "repo": "spert-story-map",

  "versionConstant": {
    "file": "src/lib/version.ts",
    "export": "APP_VERSION"
  },

  "changelog": {
    "file": "CHANGELOG.md",
    "headingPattern": "^## Version (\\d+\\.\\d+\\.\\d+)\\s*\\(",

    "_extraSurfacesNote": "public/CHANGELOG.md is NOT a backup here — it is the surface users read. src/pages/ChangelogView.tsx does fetch('/CHANGELOG.md') at runtime, which resolves to the public/ copy. The root CHANGELOG.md is the one nothing renders. If they drift, the app shows the STALE one.",

    "extraSurfaces": [
      {
        "file": "public/CHANGELOG.md",
        "match": "identical",
        "note": "Fetched at runtime by ChangelogView.tsx — this is what users see. Keep byte-identical."
      }
    ]
  },

  "claudeMdVersionPatterns": [],

  "commands": [
    { "name": "lint", "run": "npm run lint" },
    {
      "name": "typecheck",
      "run": "npm run typecheck",
      "_note": "Plain `tsc -b --force`. The ratchet (scripts/typecheck-baseline.mjs + typecheck-baseline.json) was retired in v0.50.2 once the repo reached zero — a recorded baseline number and the compiler can disagree, and the compiler is the one that matters. Kept ahead of the tests so a type error is attributed precisely instead of surfacing as a build failure; `build` below enforces the same thing again, and the two cannot disagree because both run tsc."
    },
    { "name": "test", "run": "npm test" },
    {
      "name": "build (tsc -b && vite build)",
      "run": "npm run build",
      "_note": "As of v0.50.2 `build` typechecks. This is the real gate: Vercel runs `npm run build` on every deploy, so a type error now stops the deploy instead of being stripped silently by esbuild. It took 2,183 -> 0 across v0.49.5..v0.50.1 to make this safe to turn on."
    }
  ]
}
